import streamlit as st

# ==========================================================
# 기본 설정
# ==========================================================
st.set_page_config(
    page_title="문자의 사용과 식 · 일차방정식",
    page_icon="🧮",
    layout="wide",
)

# ==========================================================
# 퀴즈 데이터
# 난이도: 하 / 중 / 상
# 정답은 모두 숫자(정수)로 입력받습니다.
# ==========================================================
QUIZ = [
    {
        "level": "하",
        "standard": "9수02-01",
        "question": "x = 3일 때, 2x + 1의 값은 얼마인가요?",
        "answer": 7,
        "hint": "x 자리에 3을 대입해서 계산해 보세요. 2×3 + 1 = ?",
    },
    {
        "level": "하",
        "standard": "9수02-01",
        "question": "x = 5일 때, x − 2의 값은 얼마인가요?",
        "answer": 3,
        "hint": "5 − 2를 계산해 보세요.",
    },
    {
        "level": "중",
        "standard": "9수02-02",
        "question": "(2x + 3) + (x − 1)을 계산하여 정리했을 때, x의 계수는 얼마인가요?",
        "answer": 3,
        "hint": "동류항(x항끼리, 상수항끼리)을 각각 더해 보세요.",
    },
    {
        "level": "중",
        "standard": "9수02-02",
        "question": "(5x − 2) − (3x + 4)를 계산하여 정리했을 때, x의 계수는 얼마인가요?",
        "answer": 2,
        "hint": "괄호를 풀 때 부호에 주의하세요. 5x − 2 − 3x − 4",
    },
    {
        "level": "하",
        "standard": "9수02-03",
        "question": "등식 3x + 2 = 11이 성립할 때, x의 값(해)은 얼마인가요?",
        "answer": 3,
        "hint": "양변에서 2를 빼고, 3으로 나누어 보세요.",
    },
    {
        "level": "중",
        "standard": "9수02-04",
        "question": "일차방정식 2x − 5 = 9를 풀면 x는 얼마인가요?",
        "answer": 7,
        "hint": "이항하여 2x = 9 + 5 로 만들어 보세요.",
    },
    {
        "level": "중",
        "standard": "9수02-04",
        "question": "일차방정식 3(x − 2) = 9를 풀면 x는 얼마인가요?",
        "answer": 5,
        "hint": "먼저 분배법칙으로 괄호를 풀어보세요. 3x − 6 = 9",
    },
    {
        "level": "상",
        "standard": "9수02-04",
        "question": "일차방정식 4x + 3 = 2x + 11을 풀면 x는 얼마인가요?",
        "answer": 4,
        "hint": "미지수 x는 좌변으로, 상수항은 우변으로 이항해 보세요.",
    },
    {
        "level": "상",
        "standard": "9수02-04",
        "question": "어떤 수의 2배에서 3을 뺀 값이 15일 때, 그 수는 얼마인가요?",
        "answer": 9,
        "hint": "어떤 수를 x라 하면 2x − 3 = 15 라는 방정식을 세울 수 있어요.",
    },
    {
        "level": "상",
        "standard": "9수02-04",
        "question": "연속하는 두 자연수의 합이 25일 때, 두 수 중 작은 수는 얼마인가요?",
        "answer": 12,
        "hint": "작은 수를 x라 하면 큰 수는 x+1 이에요. x + (x+1) = 25",
    },
]

LEVEL_COLOR = {"하": "🟢", "중": "🟡", "상": "🔴"}

# ==========================================================
# 세션 상태 초기화
# ==========================================================
def init_state():
    if "attempts" not in st.session_state:
        st.session_state.attempts = [0] * len(QUIZ)  # 문제별 시도 횟수
    if "solved" not in st.session_state:
        st.session_state.solved = [False] * len(QUIZ)  # 문제별 정답 여부(정답공개 포함 종료)
    if "feedback" not in st.session_state:
        st.session_state.feedback = [""] * len(QUIZ)  # 문제별 안내 메시지
    if "feedback_type" not in st.session_state:
        st.session_state.feedback_type = [""] * len(QUIZ)  # success / error / info


init_state()

# ==========================================================
# 사이드바 네비게이션
# ==========================================================
st.sidebar.title("📘 단원 목차")
menu = st.sidebar.radio(
    "이동할 페이지를 선택하세요",
    ["🏠 단원 소개", "1️⃣ 문자의 사용과 식", "2️⃣ 일차식의 계산", "3️⃣ 방정식과 등식의 성질", "4️⃣ 일차방정식의 풀이와 활용", "📝 단원 평가(퀴즈)"],
)

st.sidebar.markdown("---")
st.sidebar.markdown("### 🎯 성취기준")
st.sidebar.markdown(
    """
- **[9수02-01]** 다양한 상황을 문자를 사용한 식으로 나타내어 그 유용성을 인식하고, 식의 값을 구할 수 있다.
- **[9수02-02]** 일차식의 덧셈과 뺄셈의 원리를 이해하고, 그 계산을 할 수 있다.
- **[9수02-03]** 방정식과 그 해의 뜻을 알고, 등식의 성질을 설명할 수 있다.
- **[9수02-04]** 일차방정식을 풀 수 있고, 이를 활용하여 문제를 해결할 수 있다.
"""
)

# ==========================================================
# 페이지: 단원 소개
# ==========================================================
if menu == "🏠 단원 소개":
    st.title("🧮 문자의 사용과 식 · 일차방정식")
    st.subheader("중학교 1학년 수학")
    st.markdown(
        """
이 단원에서는 **문자를 사용하여 식을 세우는 방법**을 배우고, 이를 바탕으로
**일차식의 계산**과 **일차방정식을 푸는 방법**까지 차례로 학습합니다.

| 차시 | 학습 주제 | 관련 성취기준 |
|---|---|---|
| 1차시 | 문자의 사용과 식의 값 | 9수02-01 |
| 2차시 | 일차식의 덧셈과 뺄셈 | 9수02-02 |
| 3차시 | 방정식과 등식의 성질 | 9수02-03 |
| 4차시 | 일차방정식의 풀이와 활용 | 9수02-04 |
| 5차시 | 단원 평가(퀴즈) | 전체 |

왼쪽 사이드바에서 원하는 차시를 선택해 학습을 시작해 보세요. 😊
"""
    )
    st.info("💡 각 차시의 개념을 충분히 학습한 뒤, 마지막 '단원 평가(퀴즈)' 메뉴에서 실력을 확인해 보세요.")

# ==========================================================
# 페이지 1: 문자의 사용과 식 (9수02-01)
# ==========================================================
elif menu == "1️⃣ 문자의 사용과 식":
    st.title("1️⃣ 문자의 사용과 식")
    st.caption("성취기준 [9수02-01] 다양한 상황을 문자를 사용한 식으로 나타내어 그 유용성을 인식하고, 식의 값을 구할 수 있다.")

    st.header("① 문자를 사용하는 이유")
    st.markdown(
        """
같은 규칙을 가진 여러 상황을 하나의 식으로 나타내려면, 변하는 값을 대표하는 **문자**가 필요합니다.

예를 들어 한 자루에 500원인 연필을 x자루 살 때 필요한 금액은 다음과 같이 나타낼 수 있습니다.

> 연필의 가격 = 500 × x (원)

이처럼 문자를 사용하면 x자루가 1자루든 100자루든 **하나의 식으로 상황을 표현**할 수 있어서 편리합니다.
"""
    )

    st.header("② 곱셈 기호와 나눗셈 기호의 생략")
    st.markdown(
        """
문자를 사용한 식에서는 다음과 같은 **약속(생략 규칙)**을 사용합니다.

1. 수와 문자의 곱에서는 곱셈 기호 ×를 생략하고, **수를 문자 앞에 씁니다.**
   - 예) `x × 3` → `3x`
2. 문자와 문자의 곱에서는 곱셈 기호를 생략하고, 보통 **알파벳 순서**로 씁니다.
   - 예) `b × a` → `ab`
3. **같은 문자의 곱은 거듭제곱**으로 나타냅니다.
   - 예) `a × a × a` → `a³`
4. 1 또는 −1과 문자의 곱에서는 **1을 생략**합니다.
   - 예) `1 × x` → `x`,  `(−1) × x` → `−x`
5. 나눗셈 기호 ÷는 생략하고 **분수 꼴**로 나타냅니다.
   - 예) `x ÷ 2` → `x/2`
"""
    )

    st.header("③ 식의 값")
    st.markdown(
        """
문자를 사용한 식에서 **문자 대신 어떤 수를 넣는 것을 대입**이라고 하고,
대입하여 계산한 결과를 **식의 값**이라고 합니다.

**예제**: x = 4일 때, 3x + 2의 값을 구해봅시다.

1. x 자리에 4를 대입합니다 → 3 × 4 + 2
2. 곱셈을 먼저 계산합니다 → 12 + 2
3. 덧셈을 계산합니다 → **14**

따라서 x = 4일 때, 3x + 2의 값은 **14**입니다.
"""
    )
    with st.expander("🔎 함께 풀어보기: x = 2일 때, x² + 3x의 값은?"):
        st.markdown(
            """
1. x 자리에 2를 대입 → 2² + 3×2
2. 거듭제곱 먼저 계산 → 4 + 6
3. 덧셈 계산 → **10**
"""
        )

# ==========================================================
# 페이지 2: 일차식의 계산 (9수02-02)
# ==========================================================
elif menu == "2️⃣ 일차식의 계산":
    st.title("2️⃣ 일차식의 덧셈과 뺄셈")
    st.caption("성취기준 [9수02-02] 일차식의 덧셈과 뺄셈의 원리를 이해하고, 그 계산을 할 수 있다.")

    st.header("① 항, 다항식, 단항식")
    st.markdown(
        """
- **항**: 수 또는 문자의 곱으로만 이루어진 식의 한 부분 (예: 2x, −3, 5y)
- **상수항**: 문자 없이 수로만 이루어진 항
- **다항식**: 하나 이상의 항의 합으로 이루어진 식 (예: 2x + 3)
- **단항식**: 다항식 중에서 항이 1개뿐인 식 (예: 5x)
- **계수**: 문자 앞에 곱해진 수 (예: 2x에서 계수는 2)
- **차수**: 문자가 곱해진 개수 (예: x는 1차, x²은 2차)
- **일차식**: 차수가 1인 다항식 (예: 3x + 1)
"""
    )

    st.header("② 동류항과 그 계산")
    st.markdown(
        """
**동류항**이란 문자와 차수가 각각 같은 항을 말합니다. (예: 2x와 5x는 동류항, 2x와 2x²은 동류항이 아님)

동류항은 다음과 같이 **계수끼리 더하거나 빼서** 하나의 항으로 합칠 수 있습니다.

> 2x + 5x = (2+5)x = 7x

> 6y − 2y = (6−2)y = 4y
"""
    )

    st.header("③ 일차식의 덧셈과 뺄셈")
    st.markdown(
        """
일차식끼리 더하거나 뺄 때는 다음 순서로 계산합니다.

1. 괄호가 있으면 **분배법칙**을 이용해 괄호를 풉니다. (뺄셈은 괄호 안 부호가 모두 바뀝니다)
2. **동류항끼리 모아서** 계산합니다.

**예제 1 (덧셈)**: (2x + 3) + (4x − 1)
- 괄호를 풀면 → 2x + 3 + 4x − 1
- 동류항끼리 정리 → (2x + 4x) + (3 − 1)
- 계산 → **6x + 2**

**예제 2 (뺄셈)**: (5x + 2) − (3x − 4)
- 괄호를 풀면 (부호 반전) → 5x + 2 − 3x + 4
- 동류항끼리 정리 → (5x − 3x) + (2 + 4)
- 계산 → **2x + 6**
"""
    )
    st.warning("⚠️ 뺄셈에서 괄호를 풀 때 뒤 괄호 안 모든 항의 부호가 바뀐다는 점을 꼭 기억하세요!")

# ==========================================================
# 페이지 3: 방정식과 등식의 성질 (9수02-03)
# ==========================================================
elif menu == "3️⃣ 방정식과 등식의 성질":
    st.title("3️⃣ 방정식과 등식의 성질")
    st.caption("성취기준 [9수02-03] 방정식과 그 해의 뜻을 알고, 등식의 성질을 설명할 수 있다.")

    st.header("① 등식과 방정식")
    st.markdown(
        """
- **등식**: 등호(=)를 사용하여 두 수 또는 식이 같음을 나타낸 식 (예: 3 + 2 = 5, 2x + 1 = 7)
- **방정식**: 문자의 값에 따라 참이 되기도 하고 거짓이 되기도 하는 등식 (예: x + 3 = 5)
- **미지수**: 방정식에 있는 문자 (보통 x)
- **해(근)**: 방정식을 참이 되게 하는 미지수의 값
- **항등식**: 미지수에 어떤 값을 대입해도 항상 참이 되는 등식 (예: 2x = x + x)

**방정식을 푼다**는 것은 그 방정식의 **해를 모두 구하는 것**을 의미합니다.
"""
    )

    st.header("② 등식의 성질")
    st.markdown(
        """
등식은 다음과 같은 성질을 가지고 있으며, 이 성질을 이용해 방정식을 풀 수 있습니다.

| 성질 | 설명 |
|---|---|
| 성질 1 | 등식의 양변에 같은 수를 **더해도** 등식은 성립한다. → a = b 이면 a + c = b + c |
| 성질 2 | 등식의 양변에서 같은 수를 **빼도** 등식은 성립한다. → a = b 이면 a − c = b − c |
| 성질 3 | 등식의 양변에 같은 수를 **곱해도** 등식은 성립한다. → a = b 이면 ac = bc |
| 성질 4 | 등식의 양변을 0이 아닌 같은 수로 **나누어도** 등식은 성립한다. → a = b 이면 a/c = b/c (c≠0) |
"""
    )

    st.header("③ 등식의 성질을 이용해 방정식 풀기")
    st.markdown(
        """
**예제**: x + 4 = 9

1. 양변에서 4를 뺍니다 (성질 2) → x + 4 − 4 = 9 − 4
2. 정리하면 → **x = 5**

이처럼 등식의 성질을 차례로 적용하면, 미지수만 한쪽에 남기고 그 값을 구할 수 있습니다.
"""
    )

# ==========================================================
# 페이지 4: 일차방정식의 풀이와 활용 (9수02-04)
# ==========================================================
elif menu == "4️⃣ 일차방정식의 풀이와 활용":
    st.title("4️⃣ 일차방정식의 풀이와 활용")
    st.caption("성취기준 [9수02-04] 일차방정식을 풀 수 있고, 이를 활용하여 문제를 해결할 수 있다.")

    st.header("① 이항")
    st.markdown(
        """
등식의 성질을 이용하면, 등식의 한 변에 있는 항을 부호를 바꾸어 다른 변으로 옮길 수 있습니다.
이것을 **이항**이라고 합니다.

> x + 3 = 7  →  x = 7 − 3  (+3이 이항되면서 −3으로 부호가 바뀜)
"""
    )

    st.header("② 일차방정식")
    st.markdown(
        """
방정식의 우변에 있는 모든 항을 좌변으로 이항하여 정리했을 때,

> (일차식) = 0

의 꼴로 나타나는 방정식을 **일차방정식**이라고 합니다. (예: 2x − 3 = 0)
"""
    )

    st.header("③ 일차방정식을 푸는 순서")
    st.markdown(
        """
1. 괄호가 있으면 분배법칙으로 괄호를 풉니다.
2. 미지수 x가 있는 항은 좌변으로, 상수항은 우변으로 **이항**합니다.
3. 양변을 각각 정리하여 `ax = b` 꼴로 만듭니다.
4. 양변을 x의 계수 a로 나누어 **x = (b/a)** 형태로 해를 구합니다.

**예제**: 3x − 4 = 2x + 1

1. 이항 → 3x − 2x = 1 + 4
2. 정리 → x = 5
"""
    )

    st.header("④ 일차방정식의 활용")
    st.markdown(
        """
문장으로 주어진 문제를 방정식으로 해결할 때는 다음 순서를 따릅니다.

1. 구하려는 값을 **미지수 x로 놓기**
2. 문제 상황을 **방정식으로 세우기**
3. 방정식을 **풀기**
4. 구한 해가 **문제의 조건에 맞는지 확인**하기

**예제**: 어떤 수에 5를 더한 후 2배 하면 20이 될 때, 어떤 수를 구해봅시다.

1. 어떤 수를 x라 하면 → 2(x + 5) = 20
2. 괄호를 풀면 → 2x + 10 = 20
3. 이항하면 → 2x = 10
4. 계산하면 → **x = 5**
"""
    )
    st.success("✅ 활용 문제는 '무엇을 x로 놓을지'를 정확히 정하는 것이 가장 중요한 첫걸음입니다!")

# ==========================================================
# 페이지: 단원 평가(퀴즈)
# ==========================================================
elif menu == "📝 단원 평가(퀴즈)":
    st.title("📝 단원 평가 (퀴즈)")
    st.markdown(
        """
지금까지 배운 내용을 바탕으로 **10문제**를 풀어봅시다.
난이도는 🟢하 / 🟡중 / 🔴상 으로 구성되어 있습니다.

- 답은 **숫자**로 입력해 주세요.
- 오답을 입력하면 **1번의 재도전 기회**가 주어집니다.
- 재도전에서도 틀리면 **정답이 바로 공개**됩니다.
"""
    )
    st.markdown("---")

    solved_count = sum(st.session_state.solved)
    st.progress(solved_count / len(QUIZ))
    st.caption(f"진행 상황: {solved_count} / {len(QUIZ)} 문제 완료")
    st.markdown("---")

    for i, q in enumerate(QUIZ):
        st.subheader(f"문제 {i+1}. {LEVEL_COLOR[q['level']]} 난이도: {q['level']}")
        st.caption(f"관련 성취기준: {q['standard']}")
        st.markdown(f"**{q['question']}**")

        # 이미 종료된(정답 맞췄거나 정답 공개된) 문제는 입력을 비활성화
        disabled = st.session_state.solved[i]

        col1, col2 = st.columns([3, 1])
        with col1:
            user_input = st.text_input(
                "답을 입력하세요 (숫자만 입력)",
                key=f"input_{i}",
                disabled=disabled,
                label_visibility="collapsed",
                placeholder="예: 5",
            )
        with col2:
            submit = st.button("제출", key=f"submit_{i}", disabled=disabled)

        if submit and not disabled:
            try:
                user_answer = float(user_input.strip())
                is_correct = user_answer == float(q["answer"])
            except (ValueError, AttributeError):
                is_correct = False
                user_answer = None

            if is_correct:
                st.session_state.feedback[i] = "🎉 정답입니다!"
                st.session_state.feedback_type[i] = "success"
                st.session_state.solved[i] = True
            else:
                st.session_state.attempts[i] += 1
                if st.session_state.attempts[i] >= 2:
                    # 2번째 오답 → 정답 공개 후 종료
                    st.session_state.feedback[i] = (
                        f"❌ 오답입니다. 정답은 **{q['answer']}** 입니다."
                    )
                    st.session_state.feedback_type[i] = "info"
                    st.session_state.solved[i] = True
                else:
                    # 1번째 오답 → 재도전 기회 부여
                    st.session_state.feedback[i] = "❌ 오답입니다. 다시 한번 입력해주세요."
                    st.session_state.feedback_type[i] = "error"

        # 피드백 메시지 출력
        if st.session_state.feedback[i]:
            ftype = st.session_state.feedback_type[i]
            if ftype == "success":
                st.success(st.session_state.feedback[i])
            elif ftype == "error":
                st.error(st.session_state.feedback[i])
                with st.expander("💡 힌트 보기"):
                    st.write(q["hint"])
            elif ftype == "info":
                st.info(st.session_state.feedback[i])

        st.markdown("---")

    if solved_count == len(QUIZ):
        st.balloons()
        st.header("🏆 모든 문제를 완료했습니다! 수고했어요 👏")
        if st.button("🔄 처음부터 다시 풀기"):
            st.session_state.attempts = [0] * len(QUIZ)
            st.session_state.solved = [False] * len(QUIZ)
            st.session_state.feedback = [""] * len(QUIZ)
            st.session_state.feedback_type = [""] * len(QUIZ)
            st.rerun()